# La variable "primera_vez" sería un booleano que indica si
# se está accediendo por primera vez, o si simplemente
# se quiere cambiar algo de la configuración. 

def main(primera_vez):
    if primera_vez==True:
        # Main con todas las configuraciones necesarias para 
        # que funcione el código con todas sus librerías. 
        # Debería incluir cosas como la localidad, la cuenta
        # de spotify, un nombre clave, etc.
        # Sería conveniente que algunas cosas sean puestas por terminal
        # (Cómo los números de los contactos)
        
        # Los valores configurados aquí deberían ser accesibles para todo el
        # sistema.
        
        return
    else:
        # Main con un menú para elegir qué cambiar del sistema.
        # Puede ser por terminal también.
        return
